//
// Bitrise app generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import ArgumentParser
import API

// List all Bitrise organizations that the user is part of
struct OrgListCommand: AuthenticatedCommand {
    static var configuration = CommandConfiguration(
        commandName: "OrgList",
        abstract: "List the organizations that the user is part of"
    )

    @OptionGroup var auth: AuthOptions

    func run() throws {
        let request = Bitrise.Organizations.OrgList.Request()

        client
            .call(request)
            .waitUntilDone()
    }
}

